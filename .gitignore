.superpowers

# Python virtualenv — matches at any depth (root and backend/).
.venv/

# setuptools build metadata, regenerated by every `pip install -e`.
# Never hand-maintained; see backend/.gitignore for the rest of the
# Python artefacts.
*.egg-info/
