import { PageStub } from './PageStub';

export function PriceReviewPage() {
  return (
    <PageStub
      eyebrow="Импорт прайс-листа"
      title="Ревью прайс-листа"
      description="Здесь будет ревью строк, распознанных из PDF прайса поставщика — экран ещё не реализован."
    />
  );
}
